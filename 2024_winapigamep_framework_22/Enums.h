#pragma once

enum class LAYER
{
	GROUND,
	DEFAULT,
	BACKGROUND,
	PLAYER,
	PROJECTILE,
	ENEMY,
	// UI
	END = 30
};

enum class PEN_TYPE
{
	HOLLOW, RED, GREEN,
	BLUE, YELLOW, END
};

enum class BRUSH_TYPE
{
	HOLLOW, RED, GREEN,
	BLUE, YELLOW, END
};


enum class EVENT_TYPE
{
	CREATE_OBJECT,
	DELETE_OBJECT,
	SCENE_CHANGE,
	END,
};

enum class PLAYER_ANIM_TYPE
{
	IDLE,
	MOVE,
	JUMP,
};

